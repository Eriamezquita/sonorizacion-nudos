# Carpeta de salidas

- `audio/`: archivos WAV, MIDI o MP3 generados por el código.
- `figures/`: figuras y visualizaciones generadas por el código.

Por defecto, `.gitignore` evita subir audios e imágenes generadas. Para publicar resultados finales, se puede modificar esa decisión o subir solamente una selección.
